# Opus Publica RC2 — Final Baseline Anomaly Review

## 1. `supabase/.branches/_current_branch`
- **Finding**: Staged for commit. This is an internal local state file generated by the Supabase CLI to track local branch status.
- **Recommended Action**: **EXCLUDE**. Remove from the Git index (`git rm --cached`) and add `supabase/.branches/` to `.gitignore`.
- **Security Impact**: Low. Exposes local CLI state but no credentials.
- **Affects Correctness**: No.

## 2. `supabase/schema.sql`
- **Finding**: Currently untracked/unstaged as new modifications, but exists in the local filesystem. (Note: The repository already tracks an older or unmodified version, hence it does not appear in the explicit staging list). 
- **Recommended Action**: **INCLUDE**. Explicitly stage the current local file to ensure the most up-to-date historical schema dump is captured.
- **Security Impact**: None.
- **Affects Correctness**: No, it is historical evidence, not an authoritative migration.

## 3. `supabase/MIGRATE_ALL.sql`
- **Finding**: Exists locally but is absent from explicit staging. 
- **Recommended Action**: **INCLUDE**. Explicitly stage this file as it is approved historical lineage evidence.
- **Security Impact**: None.
- **Affects Correctness**: No.

## 4. `supabase/MIGRATE_ALL2.sql`
- **Finding**: Staged for commit. 
- **Recommended Action**: **INCLUDE**. This is approved historical lineage evidence.
- **Security Impact**: None.
- **Affects Correctness**: No.

## 5. `supabase/migrations/20260723000000_add_indexing_status_and_rename_peer_review.sql`
- **Finding**: Modified relative to `HEAD`. The staged/local version replaces a rigid schema mutation (`ALTER TABLE ... RENAME COLUMN`) with a robust, idempotent `DO $$ BEGIN IF EXISTS ... END $$;` block. This was explicitly modified to safely reconcile local schema drift and prevent collision failures during `db reset`.
- **Recommended Action**: **INCLUDE**. Stage this exact idempotent version.
- **Security Impact**: None.
- **Affects Correctness**: Yes. Reverting to `HEAD` would reintroduce the schema drift crash. The idempotent version is required for a successful `supabase db reset`.

## 6. `lib/types_backup.ts`
- **Finding**: Staged for commit. It appears to be a manual point-in-time backup of the generated Supabase database types.
- **Recommended Action**: **EXCLUDE**. Generated types are maintained in `lib/types.ts`. A static backup file introduces clutter and potential confusion. Unstage and leave locally or delete.
- **Security Impact**: None.
- **Affects Correctness**: No.

## 7. `lib/opce/**/*.js`
- **Finding**: Multiple `.js` files are staged alongside their `.ts` source counterparts (e.g., `lib/opce/index.js`, `lib/opce/adapters/html-adapter.js`). These are compiled machine-generated build artifacts resulting from local `tsc` execution.
- **Recommended Action**: **EXCLUDE**. Unstage all `lib/opce/**/*.js` files. They should not be tracked in source control.
- **Security Impact**: None.
- **Affects Correctness**: No.

## 8. `implementation/` Forensic Reports
- **Finding**: Numerous markdown reports (e.g., `supabase-migration-lineage-reconciliation.md`, `wp0102-authenticated-rpc-runtime-report.md`) generated during this architectural certification pass are staged.
- **Recommended Action**: **INCLUDE**. These reports constitute the formal provenance, certification evidence, and forensic trail of the RC2 preparation. They are highly valuable for the receiving engineering team.
- **Security Impact**: None. They contain no active credentials.
- **Affects Correctness**: No, but they provide essential architectural context.

---

### `RC2 BASELINE ANOMALY REVIEW COMPLETE`
